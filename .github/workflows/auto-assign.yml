name: Auto Assign Issues

on:
  issues:
    types: [opened]

jobs:
  assign:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pozil/auto-assign-issue@v2
        with:
          assignees: KibzGithub7407
